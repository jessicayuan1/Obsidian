


class ShoppingCart {

	private:
  class Item {
  
  	//data
    //item name
    //URL 
    float price;
    
    
  }
  public:
  
  array[size];
  dict;

	bool add (item.name, quantity){
  	std::cout << " success " std::endl;
    
    
    return true; // If successful
    
		array += item.name;
    dict += (name, quantity);  
    
    if (dict.name == item.name){
    	dict.name[quantity] += quantity
    }
    
    
  }
  void remove(name){ //Assuming user deletes 1 quantity
  
  	if !(array) {
    	return;
    }
    
    //array.pop(item.name);
		
		dict.name[quantity] -= 1;
    
    if (quantity) <= 0 {//Now item completely out of cart
    	dict.name.pop()
    }
    
    
    
  }
  
  
	 double reciept (  ) {
  
  	//If the user completed a checkout
		//List of item names
    
    for item in dict:
    	total += item.price;
      
    
    return total;
    
  	
  }
  
  void checkout () {
  	//Need items in the cart
    
  	
  
  
  }
}

ShoppingCart.add(Item)
//Print correct output 





if receipt () == new increase in revenue {

	success
  
}











